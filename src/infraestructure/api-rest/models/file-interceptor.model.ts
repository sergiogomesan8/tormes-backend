export enum FileInterceptorSavePath {
  PRODUCTS = './tormes/assets/images/products',
  SECTIONS = './tormes/assets/images/sections',
}

export const MAX_SIZE_VALIDATOR = 1024 * 1024 * 2;

export const FILE_TYPE_VALIDATOR = /image\/(jpeg|jpg|png)/;
